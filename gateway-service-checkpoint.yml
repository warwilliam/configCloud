server:
  port: 8080

spring:  
  cloud:
    gateway:
      default-filters:
        - name: LogFilter
      routes:
        - id: apimoviesroute
          uri: lb://MOVIES-SERVICE
          predicates:
            - Path=/api-movies/**
        - id: apicatalogroute
          uri: lb://CATALOG-SERVICE
          predicates:
           - Path=/api-catalog/**
        
